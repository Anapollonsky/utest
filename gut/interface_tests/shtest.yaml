global:
  interface: shell
  shell: sh
  print_response:
  send: pwd
  
cmd:
  store_regex:
    regexes: '/(\w+)\r\n'
    store_as: 'current_folder'

cmd:
  check_regex:
    regexes: '/(\w+)\r\n'
    check_as: 'current_folder'


  
cmd:
  send: ls /
  expect:
    - bin
    - usr
  store_regex:
    regexes: '(bin).*\r\n.*(tmp)'
    store_as: 'random'
  print_response:

cmd:
  send: ls /
  expect:
    - bin
    - usr
  check_regex:
    regexes: '(bin).*\r\n.*(tmp)'
    check_as: 'random'    
  print_response:

cmd:
  send: date
  store_regex:
    regexes: 'EDT (\d{4})'
    store_as: year
  check_number:
    input1: year
    input2:
      input1: 2017
      input2: 3
      operation: "-"
    operation: "="
  